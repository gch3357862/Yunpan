server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/yunpan?serverTimezone=UTC
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    locations: classpath:db

mybatis:
  mapper-locations: classpath:mapper/*.xml